{% if pageCount > 1 %}
    <ul class="pagination flex justify-between basis-full">
        {% if previous is defined %}
          <li class="arrow">
              <a rel="prev" href="{{ path(route, query|merge({(pageParameterName): previous})) }}" class="rounded-l-lg bg-slate-500 text-slate-100 py-1 px-2">&laquo; {{ 'label_previous'|trans({}, 'KnpPaginatorBundle') }}</a>
          </li>
        {% else %}
            <li class="arrow unavailable">
                <a>
                    &laquo; {{ 'label_previous'|trans({}, 'KnpPaginatorBundle') }}
                </a>
            </li>
        {% endif %}

        {% if startPage > 1 %}
            <li>
                <a href="{{ path(route, query|merge({(pageParameterName): 1})) }}">1</a>
            </li>
            {% if startPage == 3 %}
                <li>
                    <a href="{{ path(route, query|merge({(pageParameterName): 2})) }}">2</a>
                </li>
            {% elseif startPage != 2 %}
                <li class="unavailable">
                    <a>&hellip;</a>
                </li>
            {% endif %}
        {% endif %}

        {% for page in pagesInRange %}
            {% if page != current %}
                <li>
                    <a href="{{ path(route, query|merge({(pageParameterName): page})) }}" class="rounded-lg bg-slate-500 text-slate-100 py-1 px-2">
                        {{ page }}
                    </a>
                </li>
            {% else %}
                <li class="current">
                    <a>{{ page }}</a>
                </li>
            {% endif %}

        {% endfor %}

        {% if pageCount > endPage %}
            {% if pageCount > (endPage + 1) %}
                {% if pageCount > (endPage + 2) %}
                    <li class="unavailable">
                        <a>&hellip;</a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path(route, query|merge({(pageParameterName): (pageCount - 1)})) }}">
                            {{ pageCount -1 }}
                        </a>
                    </li>
                {% endif %}
            {% endif %}
            <li>
                <a href="{{ path(route, query|merge({(pageParameterName): pageCount})) }}" class="rounded-lg bg-slate-500 text-slate-100 py-1 px-2">{{ pageCount }}</a>
            </li>
        {% endif %}

        {% if next is defined %}
            <li class="arrow">
                <a rel="next" href="{{ path(route, query|merge({(pageParameterName): next})) }}" class="rounded-r-lg bg-slate-500 text-slate-100 py-1 px-2">
                    {{ 'label_next'|trans({}, 'KnpPaginatorBundle') }} &nbsp;&raquo;
                </a>
            </li>
        {% else %}
            <li class="arrow unavailable">
                <a>
                    {{ 'label_next'|trans({}, 'KnpPaginatorBundle') }} &nbsp;&raquo;
                </a>
            </li>
        {% endif %}
    </ul>
{% endif %}
