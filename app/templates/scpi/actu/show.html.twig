{% extends 'scpi/scpi_base.html.twig' %}

{% block title %}{{ actu.title }}
{% endblock %}

{% block content %}
	<div class="pb-16">
		<h1 class="bg-red-600 w-1/2 mx-auto mt-0 pt-6 pb-4 rounded-b-3xl text-white text-center text-4xl font-semibold">{{ actu.title }}</h1>

		<div class="bg-white shadow-md border-0 rounded-md my-4 mx-auto max-w-4xl p-8">
			<div class="prose prose-lg max-w-none">
				{{ actu.content|raw }}
			</div>

			<div class="mt-8 pt-6 border-t border-gray-200">
				<a href="{{ path('scpi_hp') }}" class="inline-block px-6 py-3 bg-red-600 text-white rounded hover:bg-red-700 transition-colors">
					<i class="fa-solid fa-angles-left mr-2"></i>Retour à l'accueil
				</a>
			</div>
		</div>
	</div>
{% endblock %}
