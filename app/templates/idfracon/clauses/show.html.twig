{% extends 'base.html.twig' %}

{% block title %}Clause
{% endblock %}

{% block content %}
	<div class="rounded-lg bg-slate-200 p-5">
  <div class="flex justify-between">
		<h1>Clause N°{{ clause.id }}</h1>
     {{ include('idfracon/clauses/_delete_form.html.twig') }}</div>
		<div class="flex flex-col">
			<div class="flex justify-start">
					<span class="text-xl"> {{ clause.name }}</span></div>
			</div>
			<h3>Description :</h3>
			<div>{{ clause.description }}</div>
		</div>

		<div class="flex justify-around w-full">
			<a href="{{ path('app_clauses_index') }}">
				<div class="btn btn-primary w-44">
					<i class="fa-solid fa-angles-left"></i>
					Retour</div>
			</a>

			<a href="{{ path('app_clauses_edit', {'id': clause.id}) }}">
				<div class="btn  btn-secondary w-40">
					<i class="fa-solid fa-pen"></i>
					edit</div>
			</a>
		</div>
	</div>

{% endblock %}
