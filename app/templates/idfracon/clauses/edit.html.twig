{% extends 'base.html.twig' %}

{% block title %}Edit Clauses
{% endblock %}

{% block content %}
	<div class="rounded-lg bg-slate-200 p-5">
		<div class="flex justify-between">
			<h1>Edit Clauses</h1>
			{{ include('idfracon/clauses/_delete_form.html.twig') }}
    </div>
		{{ include('idfracon/clauses/_form.html.twig', {'button_label': 'Update'}) }}
	</div>

	<div class="flex justify-around w-full">
		<a href="{{ path('app_clauses_index') }}">
			<div class="btn btn-primary w-44">
				<i class="fa-solid fa-angles-left"></i>
				Retour à la liste</div>
		</a>
	</div>
{% endblock %}
